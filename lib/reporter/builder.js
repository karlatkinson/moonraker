var fs         = require('fs'),
    config     = require('moonraker').config,
    reporters  = require('moonraker').reporters,
    path       = require('path'),
    glob       = require('glob'),
    Handlebars = require('handlebars'),
    wrench     = require('wrench'),
    util       = require('util'),
    i18n       = require('./i18n'),
    resultsDir,
    reporterPath;

module.exports.createHtmlReport = function(data) {
 
};
